
if [[ "$1" == "a" ]]
then
	path=$(nix build .#webfiles -L -v --builders "$acern" --print-out-paths --max-jobs 0)
else
	path=$(nix build .#webfiles -L -v --print-out-paths)
fi

echo path: $path

scp $path/* ocih:host/webserver/webfiles


